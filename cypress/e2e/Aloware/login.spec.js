describe("Login related scenarios", () => {
  it("Check that is not possible log in with invalid credentials", () => {

  })

  it("Validate that forgot password does not work for email not registered", () => {
    //valid user not available
  })

})

